import { Routes } from '@angular/router';
import { DescriptionsComponent } from './main-content/descriptions/descriptions.component';

export const routes: Routes = [
    { path: 'description', component: DescriptionsComponent }
];
